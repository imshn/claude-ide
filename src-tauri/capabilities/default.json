{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core window access plus native folder picker.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
